  <div class="flex w-full justify-between mt-6">
    <% if @prev_page %>
      <%= link_to 'PREVIOUS PAGE ', comics_path(page: @prev_page, character: @character), 
                                    class: "bg-white text-red-600 hover:bg-red-600 hover:text-white px-4 py-2" %>
    <% else %>
      <span></span>
    <% end %>

    <%= link_to 'NEXT PAGE', comics_path(page: @next_page, character: @character), 
                             class: "bg-white text-red-600 hover:bg-red-600 hover:text-white px-4 py-2" %>
  </div>